version: '3.8'

services:
  php:
    build: ./php
    volumes:
      - ./../:/var/www/laravel

  nginx:
    build: ./nginx
    volumes:
      - ./../:/var/www/laravel
      - ./nginx/conf/default.conf:/etc/nginx/conf.d/default.conf
    ports:
      - 81:80
    depends_on:
      - php
